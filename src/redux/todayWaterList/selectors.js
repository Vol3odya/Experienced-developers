export const selectLoading = (state) => state.loading;
export const selectError = (state) => state.error;

export const selectAmountToday = (state) => state.today;

export const selectPercentFromNorm = (state) => state.percentFromNorm;
