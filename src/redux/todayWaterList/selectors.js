export const selectLoading = (state) => state.todayWaterList.loading;
export const selectError = (state) => state.todayWaterList.error;

export const selectAmountToday = (state) => state.today;

export const selectPercentFromNorm = (state) => state.todayWaterList.percentFromNorm;
